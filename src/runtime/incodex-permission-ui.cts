// The builder embeds the native presenter and motion modules once here.
// Both the legacy Electron entry and the CLI host load this sealed sibling.
const permissionUi = "__INCODEX_ACCESSIBILITY_WINDOW__";
export = permissionUi;
