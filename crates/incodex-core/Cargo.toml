[package]
name = "incodex-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Paths, locks, JSON, and hashes for the Incodex native CLI"

[dependencies]
sha2.workspace = true
